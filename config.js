module.exports = {
  // set it to true to let the script check if it "can" send messages without really sending them
  TEST_SAFELY: false,

  // the delay after every message sending
  CHECK_DELAY: 1500,

  // the title of the "Search or start a new chat" input field (see the input.jpg file)
  findInputTitle: "Cerca o inizia una nuova chat"
};
